registered_users = {
    'Kena': 'truc',
    'shapeR': 'truc1;',
    }
registered_users_lower = [user.lower() for user in registered_users]

libraries = []
#The list's books must evolve in a list of dictionaries or a class
books = ['gaelic', 'metaphysique', 'sql', 'chiens de chasse', 'python crash course']
